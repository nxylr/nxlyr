pipecat-ai[deepgram,openai,elevenlabs,silero,websocket]==1.5.0
python-dotenv
requests
aiohttp

# fastapi arrives via the [websocket] extra above, but uvicorn does NOT — checked
# against pipecat-ai 1.5.0's own METADATA, where uvicorn appears only under
# `extra == "runner"`. We build the Exotel transport manually instead of going
# through the runner's create_transport(), so we don't take the runner extra and
# must pin uvicorn ourselves for server.py. (The `websockets` package is an
# unconditional core dep of pipecat-ai, so it needs no line here.)
uvicorn>=0.32.0,<1.0.0

# Only ever arrived transitively via pipecat-ai's [openai] extra before.
# prompt_harness.py (Task 2.3) imports it directly for plain chat-completion
# calls, so it needs its own pin rather than riding on pipecat's choice.
openai==2.44.0

# First Python Redis client in the project. Redis has been a docker-compose
# service since Week 3 (and `agent` already depends_on it), but nothing spoke to
# it until tools.py needed to write end_call_tool_invoked for Flow 3's drop
# detection. >=5 because that's where redis.asyncio landed as the supported
# async client (the separate aioredis package is long dead).
redis>=5.0.0,<7.0.0
